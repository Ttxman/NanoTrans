<UserControl x:Class="NanoTrans.SpeakerAttributeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:my="clr-namespace:NanoTrans"
             d:DesignHeight="25" d:DesignWidth="300"
             Name="control"
             >
    <UserControl.Resources>
        <ObjectDataProvider x:Key="setup" ObjectInstance="{x:Static my:GlobalSetup.Refresher}"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ComboBox Grid.Column="0" Text="{Binding ElementName=control,Path=Attribute.Name}" ItemsSource="{Binding Source={StaticResource setup}, Path=Setup.SpeakerAtributteCategories}" />
        <TextBox Grid.Column="1" Text="{Binding ElementName=control,Path=Attribute.Value}"  />
    </Grid>
</UserControl>
