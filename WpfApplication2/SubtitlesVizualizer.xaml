<UserControl x:Class="NanoTrans.SubtitlesVizualizer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="283" d:DesignWidth="677" xmlns:my="clr-namespace:NanoTrans" MouseWheel="UserControl_MouseWheel" SizeChanged="UserControl_SizeChanged">

    <UserControl.Resources>
        <ObjectDataProvider x:Key="provider" ObjectInstance="{x:Static my:MySetup.Refresher}"/>
    </UserControl.Resources>

    <Grid ClipToBounds="True">
        <Grid.Background>
                <Binding Source="{StaticResource provider}" Path="Setup.BarvaTextBoxuOdstavce"/>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="282*" />
            <ColumnDefinition Width="18" />
        </Grid.ColumnDefinitions>
        <ScrollBar Name ="gridscrollbar" Grid.Column="1" HorizontalAlignment="Right" Width="18" SmallChange="2" LargeChange="100" Maximum="200" Minimum="0" Value="200" RangeBase.ValueChanged="gridscrollbar_ValueChanged" ViewportSize="20" />
        <StackPanel Name="gridstack">
            <my:Element Height="0" x:Name="element1" Width="1" />
        </StackPanel>
    </Grid>
</UserControl>
